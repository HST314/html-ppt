<!-- TASK-001: 新增——页面逻辑模式库（页面语义分析层的“内容→结构”映射） -->
# PAGE LOGIC PATTERNS

本文件是「页面语义分析层」的查表依据：**什么样的内容 → 使用什么样的页面结构**。

使用方式：
1. <!-- TASK-003 -->先完成页面任务定义（执行流程总线第③步）：每页登记唯一页面任务（提出判断 / 展示证据 / 解释机制 / 请求决策 / 过渡收束），写不出任务的页回炉拆并。<!-- TASK-008 -->同一步内完成页面场景判定（10 类，见 `references/design-scenario-system.md`；页面任务决定场景），判定结论登记入 `state/page_semantics.md`「页面场景」列，作为本层逻辑判断与后续 pattern 选型的上游约束。
2. 对每页完成五步判断（主结论 / 信息层级 / 逻辑关系 / 视觉结构 / 视觉焦点）。
3. 在本文 13 类逻辑中选定**唯一主导逻辑**，查表得到视觉结构与组件库 role。
4. 按信息分组规则把 ≥4 条信息归为 2–4 个视觉信息组，把组落到结构区块上。
5. 把结论登记到 `state/page_semantics.md`（含「页面任务」列），并将显式 `<!-- role: xxx -->` 写回 deck.md。
6. <!-- TASK-007 fix: 蓝图口径同步指针 -->后续视觉蓝图落盘（总线⑤）的字段口径以 `references/html-layout-system.md` §0 合并蓝图定义为唯一准绳（蓝图九字段：页面类型/主视觉区域/标题区域/内容区域/辅助区域/图片区域/留白区域/SVG需求/选型理由）；本层「视觉焦点」即蓝图「主视觉区域」，禁止另立字段口径。

铁律：
- 禁止未经查表直接使用“标题 + 多条横向 bullet”版式。
- 一页一个主导逻辑；出现第二个主导逻辑时拆页。
- 结构必须承载语义：观众只看版面骨架（不看文字）也应能读出条目之间是并列、先后还是主次。

---

## 0. 通用决策流程

```
每页信息条目
  │
  ├─ ⓪ 定页面任务（TASK-003，总线第③步）：提出判断 / 展示证据 / 解释机制 / 请求决策 / 过渡收束，
  │      一页一个任务，写不出 → 拆页/并页；任务决定本页在故事线中的角色，先于结构选择
  │      <!-- TASK-008 -->同步判定页面场景（10 类，见 design-scenario-system.md）：任务决定场景，场景约束选型
  │
  ├─ ① 写主结论：一句话，写不出 → 拆页/并页
  │
  ├─ ② 数条目：<4 条 → 直接进入 ④ 选逻辑
  │            ≥4 条 → ③ 先分组
  │
  ├─ ③ 信息分组（强制 2–4 组，规则见第 15 节）
  │
  ├─ ④ 判逻辑：条目/信息组之间是什么关系？→ 查 13 类对照表（第 1–13 节）
  │      平等无先后 → 并列        两方对照   → 对比
  │      原因推结果 → 因果        步骤有先后 → 流程
  │      绑定日期   → 时间轴      程度渐强   → 递进
  │      一核多点   → 中心辐射    首尾相接   → 闭环
  │      两维交叉   → 矩阵        上下包含   → 层级
  │      数字为证   → 数据        物为主体   → 产品展示
  │      场景故事   → 图文叙事
  │
  └─ ⑤ 定视觉焦点：全页唯一锚点（最大字号 / 最大面积 / 最高对比，三者至少居一）
```

### 13 类逻辑 → 组件库 role 速查

| 逻辑类型 | 首选 role | 备选 role | 视觉焦点落点 |
| --- | --- | --- | --- |
| 并列 | two-column | bullets（受限，须附理由）、gallery、kpi | 组标题行 / 首卡片 |
| 对比 | compare | two-column | 对照分界点 |
| 因果 | two-column | compare | 结果区 |
| 流程 | timeline | two-column（编号步骤卡） | 第一节点或终点成果 |
| 时间轴 | timeline | — | “现在”节点 |
| 递进 | timeline | two-column（阶梯条） | 最高一级 |
| 中心辐射 | image-hero | two-column（中心+分支） | 中心核 |
| 闭环 | two-column | bullets（环列，受限） | 循环起点即终点 |
| 矩阵 | table | two-column（2×2 卡片） | 目标象限 |
| 层级 | table | two-column（分层条 L1…Ln） | 最高层 L1 |
| 数据 | kpi | table | 核心数字 |
| 产品展示 | image-hero | image-side、gallery | 产品主体 |
| 图文叙事 | image-side | image-hero | 场景主图 |

`cover` / `toc` / `section` / `closing` / `quote` 为叙事骨架页，不参与 13 类逻辑判定。

---

## 1. 并列

- **适用内容特征**：多个条目地位平等、无先后顺序、无因果推导；典型为“三类需求 / 四项原则 / 若干特性”。条目可互换位置而不改变含义。
- **识别信号**：“A、B、C 三类”“同时满足”“包括以下方面”。
- **结构示意**：
  ```
  ┌────────── action title（主结论）──────────┐
  │ ┌─组1─┐ ┌─组2─┐ ┌─组3─┐   ← 2–4 个等宽卡片 │
  │ │标题 │ │标题 │ │标题 │      组标题可见     │
  │ │2–4条│ │2–4条│ │2–4条│                    │
  │ └─────┘ └─────┘ └─────┘                    │
  │ takeaway 强调栏（可选，无固定前缀）           │
  └────────────────────────────────────────────┘
  ```
- **role 映射**：2 组用 `two-column`；3–4 组用卡片网格（two-column 双行或 kpi 式块）；纯图并列用 `gallery`。
- **视觉焦点**：第一个信息组的组标题，或主结论本身。
- **避免**：超过 4 组仍硬排一页；把有先后的内容伪装成并列。

## 2. 对比

- **适用内容特征**：两方（或前后两态）在同一维度上对照：方案 A vs 方案 B、before vs after、竞品 vs 本品、徽章（近观收藏）vs 海报（远观传播）。
- **识别信号**：“对比”“vs”“之前/之后”“优势”“两种尺度”。
- **结构示意**：
  ```
  ┌────────── action title ──────────┐
  │ ┌──── A 侧 ────┐ ‖ ┌──── B 侧 ────┐│
  │ │ 维度1 描述    │ ‖ │ 维度1 描述    ││
  │ │ 维度2 描述    │ ‖ │ 维度2 描述    ││  ← 同一基线逐行对照
  │ └─────────────┘ ‖ └─────────────┘│
  │ 结论条：对照后唯一判断（焦点）              │
  └──────────────────────────────────┘
  ```
- **role 映射**：`compare` 首选；维度多于 4 行时改 `table`（左列维度、右两列对象）。
- **视觉焦点**：对照后的结论条；或差异最大的一行。
- **避免**：三方以上塞进对比结构（改矩阵或并列）；只列两面事实却不给对照结论。

## 3. 因果

- **适用内容特征**：条目间存在推导链“因为 … 所以 …”：问题→根因→对策、投入→产出、策略→结果。
- **识别信号**：“由于”“因此”“导致”“源于”“带来了”。
- **结构示意**：
  ```
  ┌────────── action title ──────────┐
  │ ┌─因───────┐   ┌─果───────────┐ │
  │ │ 原因/条件  │ → │ 结果/影响（焦点）│ │
  │ │ 可多条分组 │   │ 落到主结论     │ │
  │ └─────────┘   └─────────────┘ │
  └────────────────────────────────┘
  ```
- **role 映射**：`two-column`（左因右果）；单因果+强数字结论可用 `kpi` 承载结果区。
- **视觉焦点**：结果区（观众记住的是“所以怎样”）。
- **避免**：因果链超过三级（拆成两页或改流程）；把相关关系写成因果。

## 4. 流程

- **适用内容特征**：完成一件事的有序步骤，步骤可编号：概念→深化→制作→安装→验收；设计→打样→量产→运输。
- **识别信号**：“第一步/第二步”“流程”“环节”“先…再…最后”。
- **结构示意**（参考视觉：编号胶囊链，节点 01→05 依次咬合）：
  ```
  ┌────────── action title ──────────┐
  │ [01 步骤]→[02 步骤]→[03 步骤]→[04 步骤] │
  │   说明       说明       说明       说明   │
  │ 编号显著、方向箭头显著；终点成果为焦点       │
  └──────────────────────────────────┘
  ```
- **role 映射**：`timeline`（3–6 节点）承载编号步骤；步骤含丰富图文时用 `two-column` 编号步骤卡。
- **视觉焦点**：终点节点的交付物，或当前所处节点。
- **避免**：节点超 6 个（合并为阶段或拆页）；把无顺序的并列硬编号。

## 5. 时间轴

- **适用内容特征**：信息强绑定日历时间：过去—现在—未来、roadmap、里程碑、行业演进 2003→2023。
- **识别信号**：具体年份/季度/日期，“历程”“规划”“演进”。
- **结构示意**（参考视觉：时间长河——一条主轴穿起各时期，节点挂事件，"现在"放大）：
  ```
  ┌────────── action title ──────────┐
  │ 过去──────[现在]──────未来          │
  │ 2003 事件   2026 事件(焦点)  2027 计划 │
  │ 节点必须含：日期＋事件＋结果           │
  └──────────────────────────────────┘
  ```
- **role 映射**：`timeline`。每个节点必须含日期 + 事件 + 结果（沿用 NARRATIVE.md 格式 `2026-03｜上线灰度｜投诉率下降 18%`）。
- **视觉焦点**：“现在”或下一个里程碑节点。
- **避免**：节点无日期（说明不是时间轴，改流程或递进）。

## 6. 递进

- **适用内容特征**：程度、深度或阶段逐级加强：认识→相信→理解→体验；材料→器件→电路→算法→系统验证（能力链）。
- **识别信号**：“从…到…再到…”“逐步加深”“层层递进”“升级”。
- **结构示意**（参考视觉：阶梯/层级链，每级高于前一级，末级最大）：
  ```
  ┌────────── action title ──────────┐
  │ 级1 → 级2 → 级3 → 级4 → [级5 焦点]   │
  │ 每级：名称＋一句"比上一级多了什么"      │
  │ 视觉高度/明度逐级抬升                 │
  └──────────────────────────────────┘
  ```
- **role 映射**：`timeline`（节点按递进排序）或 `two-column` 阶梯条；层级语义更强调“上下包含”时改用第 10 节「层级」。
- **视觉焦点**：最高一级（终点状态）。
- **与流程的区别**：流程回答“先做哪步”，递进回答“深到哪层”；步骤可并行展开时用流程，程度单向累积时用递进。

## 7. 中心辐射

- **适用内容特征**：一个核心概念派生多个应用/触点/领域：同一视觉 DNA 连接徽章、海报、体验；共同感知原点走向多领域智能。
- **识别信号**：“核心是…，辐射到…”“统一…多触点”“一个原点，多个场景”。
- **结构示意**（参考视觉：中心原点 + 四周气泡分支）：
  ```
  ┌────────── action title ──────────┐
  │      分支A        分支B            │
  │        \        /                │
  │        [中心核：焦点]              │
  │        /        \                │
  │      分支C        分支D            │
  │ 中心核最大；分支 2–4 个、体量一致     │
  └──────────────────────────────────┘
  ```
- **role 映射**：`image-hero`（中心核用大图/大数字容器，分支以标注环绕）；纯文字用 `two-column`（左中心核、右分支清单）。
- **视觉焦点**：中心核。
- **避免**：分支超过 5 个（先分组）；分支之间有先后（改流程）。

## 8. 闭环

- **适用内容特征**：链路首尾相接形成循环：参观体验三重价值闭环（决策认同→公众理解→讲述传递→再强化决策）、PDCA、运营增长飞轮。
- **识别信号**：“闭环”“循环”“飞轮”“相互强化”“回到起点”。
- **结构示意**：
  ```
  ┌────────── action title ──────────┐
  │        节点1 → 节点2              │
  │        ↑        ↓                │
  │   [中心：循环主张]  节点4 ← 节点3    │
  │ 首尾箭头必须闭合；节点 3–4 个        │
  └──────────────────────────────────┘
  ```
- **role 映射**：`two-column`（一侧环列节点、一侧中心主张）；节点含数字时用 `kpi` 围合。若环节是固定先后顺序且无回授语义，改用「流程」。
- **视觉焦点**：中心循环主张；或循环中价值最高的一环。
- **避免**：环节超过 4 个（合并为阶段）；把单向链条画成环（虚假闭环）。

## 9. 矩阵

- **适用内容特征**：信息由两个维度交叉定位：重要性×紧急性、受众×场景、成本×效果。
- **识别信号**：“两个维度”“象限”“组合”“矩阵”。
- **结构示意**：
  ```
  ┌────────── action title ──────────┐
  │        维度B高                    │
  │   象限2      │   [象限1 焦点]      │
  │ ─────────────┼───────────── 维度A │
  │   象限3      │    象限4           │
  │        维度B低                    │
  └──────────────────────────────────┘
  ```
- **role 映射**：2×2 卡片网格用 `two-column` 双行；维度取值多于 2 档用 `table`（行列双维度）。
- **视觉焦点**：主结论指向的目标象限（通常是右上）。
- **避免**：维度超过两个（改层级或并列）；象限内只放一个格子有内容（信息量不足，改并列）。

## 10. 层级

- **适用内容特征**：信息存在上下包含或高低秩序：L1 基底→L4 灯光的材料分层、战略→战术→执行、金字塔能力模型。
- **识别信号**：“最上层/底层”“分层”“等级”“L1/L2/L3”“由…支撑”。
- **结构示意**（参考视觉：分层条 L1–L4 显式编号，自上而下展开；或金字塔）：
  ```
  ┌────────── action title ──────────┐
  │ L1 ── 最高层（焦点）──────────────  │
  │ L2 ──── 展开层 ────────────────    │
  │ L3 ────── 支撑层 ──────────────    │
  │ L4 ──────── 基础层 ────────────    │
  │ 每层：编号＋名称＋一句作用            │
  └──────────────────────────────────┘
  ```
- **role 映射**：`table`（层级编号列 + 名称列 + 作用列）或 `two-column`（左层级示意图、右 L1–Ln 分层条）。
- **视觉焦点**：最高层 L1（或金字塔尖）。
- **避免**：层级超过 5 层（合并）；层与层只是并列关系（改并列）。

## 11. 数据

- **适用内容特征**：页面的证据主体是数字：KPI、目标对比、同比环比、基准测试、经营结果。
- **识别信号**：多个百分比/倍数/绝对值，“提升/下降/达成”“对比目标”。
- **结构示意**：
  ```
  ┌────────── action title ──────────┐
  │  [大数字 焦点]   数字2      数字3    │
  │  说明+上下文    说明       说明     │
  │  每个数字必须带：目标对比/同比/基准   │
  └──────────────────────────────────┘
  ```
- **role 映射**：`kpi`（3–5 项）；行列规整的明细用 `table`（末行高亮结论）。
- **视觉焦点**：最大的那个核心数字。
- **避免**：数字无上下文（必须带对比口径）；KPI 超过 5 个（先分组或改表格）。

## 12. 产品展示

- **适用内容特征**：页面的主角是具体物/界面/作品：纪念徽章、海报、仪表盘截图、工艺细节。
- **识别信号**：有高权重产品图/截图，文字在解释“它是什么、好在哪”。
- **结构示意**（参考视觉：英雄主体 + 证据条——大主体占据视觉中心，底部/侧部一排 3–4 个验证点）：
  ```
  ┌────────── action title ──────────┐
  │ ┌───────────────┐  卖点1          │
  │ │  产品主体大图    │  卖点2          │
  │ │  （焦点）       │  卖点3          │
  │ └───────────────┘                │
  │ [证据1] [证据2] [证据3] [证据4] 验证条 │
  └──────────────────────────────────┘
  ```
- **role 映射**：`image-hero`（横版主体大图）或 `image-side`（竖图/截图）；多角度细节用 `gallery`；截图必须触发 `screenshot-frame`。
- **视觉焦点**：产品主体图。
- **避免**：图片出血、叠字（沿用 PROJECT_IMAGES.md 硬约束）；卖点超过 4 条不分组。

## 13. 图文叙事

- **适用内容特征**：用一个场景/人物/故事承载观点，图与文互为注解：品牌墙实景 + 设计理念、人物故事 + 语录、客户案例现场。
- **识别信号**：有场景照片且文字是“设计理念/故事/解读”，而非规格参数。
- **结构示意**（参考视觉：图锚 + 信息栏——左侧大图定场，右侧信息面板分层解读）：
  ```
  ┌────────── action title ──────────┐
  │ ┌────────────┐  栏标题            │
  │ │  场景主图    │  解读段 1         │
  │ │  （焦点）    │  解读段 2         │
  │ │  +caption   │  解读段 3         │
  │ └────────────┘  ─── 原则条 ───    │
  └──────────────────────────────────┘
  ```
- **role 映射**：`image-side`（图 1:1 或竖图）或 `image-hero`（横版场景大图 + 相邻文字）；纯语录观点用 `quote`。
- **视觉焦点**：场景主图；信息栏首段承接图的内容。
- **避免**：图文无关（图必须被文字引用）；信息栏写成参数表（那是产品展示或数据）。

---

## 14. 视觉焦点通用规则

1. 每页有且仅有一个视觉焦点：主结论数字 / 主图 / 中心节点 / 第一流程节点 / 最高层级。
2. 焦点必须至少占据以下之一：最大字号、最大面积、最高对比（ accent 色或明暗反差）。
3. 其余元素承担视线引导：编号、箭头、动线、明度递减，把视线从焦点带向次重点。
4. QA 检查：遮挡文字后只看版面骨架，3 秒内能指出焦点并说出逻辑类型，否则判不合格。

## 15. 信息分组规则（单页 ≥4 条强制）

1. **触发**：正文信息条目 ≥4 条（列表项、段落、卡片、图表标注均计入）。
2. **分组维度优先级**：对象（谁）→ 阶段（何时）→ 属性（哪类）→ 价值（为何）。按首个能把条目分成 2–4 组的维度归组。
3. **组约束**：每组 2–4 条；每组提炼一个名词短语组标题（≤10 字），组标题必须在页面上可见。
4. **组间关系决定主导逻辑**：组与组平等 → 并列；有先后 → 流程/递进；有主次 → 层级/中心辐射；两两对照 → 对比。
5. **落版**：每组对应一个独立视觉区块（卡片 / 栏 / 层 / 节点 / 象限），组内条目再排主次；区块之间留白或分隔线明确。
6. **超限处理**：归组后 >4 组、单组 >4 条、或组标题提炼不出 → 拆页；禁止压缩字号、减少留白硬塞。

## 16. 参考方案版式原则提炼（视觉表达借鉴）

以下来自品牌空间设计方案（走廊文化墙全案）的版式逻辑，作为 13 类结构的视觉表达参考，不替代组件库 role，只约束“结构如何被看见”：

1. **单一焦点**：封面与每一页都只有一个主视觉锚点（大标题 / 主体装置），其余元素退后。
2. **路径化阅读**：用连续曲线 / 编号 01–05 / W01–W07 把多节点串成一条视线动线，观众按编号顺序阅读，流程、时间轴、递进页必须显式给出阅读方向。
3. **图锚 + 信息栏**：大幅场景图定场，右侧信息面板按“标题 / 规格 / 理念 / 机制”分层解读——图文叙事与产品展示页的默认骨架。
4. **显式编号建立秩序**：L1–L4（层级）、01–05（流程）用编号把层级与顺序外化，观众不看正文也知道结构与先后。
5. **结构即隐喻**:时间长河(时间轴)、原点发散(中心辐射)、层叠爆炸图(层级)——让版面骨架本身表达逻辑关系,而不是用文字解释关系。

---

<!-- TASK-001: 新增——对标 guizang / html-ppt-skill / baoyu-design / autoppt 后的文本层升级规则(结论来源见 BENCHMARK.md) -->
## 17. 文本内部升级与符号视觉引导

页面结构选定之后,文本层必须再做一遍升级:让观众在 3 秒内抓到关键词、数据与阅读方向。本节约束"文字如何被看见",不改变结构选型。

### 17.1 关键词与数据突出(每页 1–3 处,禁止满页高亮)

| 手法 | 组件 | 用法 |
| --- | --- | --- |
| 关键词高亮 | `.hl` | 条目前缀名词/结论词,accent 色 + 加粗;每卡 ≤3 处 |
| 数据突出 | `.num` | 数字/批次/日期,accent 色 + 等宽 + 加粗 + 禁换行;单位缩小并降不透明度 |
| 数据三段式 | kpi / 行内 | label(mono 小字)→ 巨数 → note(口径对比),数字必须带口径 |

- 深底页面高亮用 accent(亮金/亮蓝),浅底用深 accent;高亮与正文对比度必须 ≥4.5:1。
- 行内高亮禁止打断行高:只用 color/weight,不用背景块硬包;需要底色时用 `box-decoration-break: clone`。

### 17.2 符号视觉引导(纯 CSS 轻量件优先)

| 符号 | 承载 | 语义 |
| --- | --- | --- |
| 编号徽章 01–09 | mono 字重 900,accent 描边/底色 | 流程顺序、目录章节、层级序号 |
| `→` / `↓` | accent-2 色字符 | 因果方向、动线引导、链路推进 |
| 1px 发丝线 / 短分隔 | `--line` token | 分组边界,禁止粗重分隔条 |
| 中央分界 + 徽标 | 竖线 + VS/节点圆 | 对比页两侧对位 |
| 环形箭头 / 回授符 | SVG 或字符 ↺ | 闭环语义的唯一可信表达 |

- 编号、箭头、分界必须**承担逻辑语义**(顺序/方向/对照),禁止纯装饰性符号。
- 同一页符号体系不超过 2 种(如编号 + 箭头),防止视觉噪音。

### 17.3 结论条(takeaway)纪律

<!-- TASK-043: 本节此前写"内容页必须有底部结论条,以 So-what：开头"，与 NARRATIVE.md 当前的
     takeaway 政策（默认不生成、不渲染，只有确有超出导语的增量信息时才显式登记，留空是正常
     状态）互相矛盾，是 build_ir.py 多处硬编码 "So-what：..." 文案的根因之一（已在 build_ir.py
     清除，此处同步改口径，禁止死灰复燃）。 -->
- 结论条**不是必需项**，默认留空；只有页面确有超出标题/导语的增量信息（新事实、新数字、新风险提示，而非换个说法再讲一遍结论）时，才用 `<!-- takeaway: ... -->` 显式登记，口径见 `NARRATIVE.md`「页面级硬规则」。
- **禁止使用 `So-what：`或任何同义固定前缀**；文案直接写增量信息本身，**禁止机械复制首条 bullet 或复述导语**。
- 结论条如出现，是全页除标题外唯一的 accent 面元素;其上文字与底色对比度 ≥4.5:1(accent 面用 `--on-accent` 反色墨字,禁止白字压浅金)。

### 17.4 克制卡片纪律(发布会式)

- 卡片 = 1px 发丝边框(token `--line`)+ 小圆角 + 轻/无阴影;禁止"圆角 + 左侧色条"卡片、禁止蓝紫渐变、禁止 emoji。
- 同页多张并列卡统一底色;只允许一个 accent 焦点面(单焦点)。
- 卡距 24–26px 一档,卡内文字预留 10–15% 余量;标题/徽章禁换行,先缩字不溢出。

### 17.5 残留文本清理(逐页强制)

- 逐页核对:与页面叙事任务无关的句子(模板腔、占位列、串场废话)必须删除或重写为项目相关内容。
- 水印字、eyebrow、meta-line 属于页面语义的一部分:必须与当页主题一致(如封面水印呼应项目代号),禁止无意义词(如 DECK/LOREM)。

<!-- TASK-023: 新增——图文复述是本项目实测出现过的高频缺陷(gallery 角色正文
     列表逐条复述下方每张图自己的说明文字,页面读起来是"同一句话看两遍"),
     C2 删除测试没有把它列为具体触发条件,导致生成阶段和 QA 阶段都放过。
     在此显式立规则,并回填 final-quality-check.md C2 判定标准引用本节。 -->
### 17.6 图文避免重复表达(gallery / image-side / image-hero / two-column 强制)

- **判定信号**:正文 `content-list`(或段落)每一条目,与其对应的图片说明(caption-bar 文案、gallery-index 图注、或图片紧邻的小标题)在指称对象和结论上高度重合——即遮住图片说明,单看正文列表也能一字不差猜出图片在讲什么 → 命中重复表达。
- **处置原则**:图片能自己说清楚的,不再用正文复述一遍;二者只能有一个承担"介绍这件东西"的职责:
  1. **优先把信息并入图片说明**:每张图的 caption-bar/图注直接携带该条要点(是什么 + 好在哪),正文不再重复同一层信息。
  2. **正文改写为图片说明之上的一层判断**:正文只留一句能把全部图片串起来的**总起结论**(如"5 款作品共同支撑同一套语言,差异体现在工艺路线"),不再逐条复述每张图各自的内容。
  3. 两种方式二选一即可解决;**禁止同时保留"正文逐条复述 + 图片自带说明"这种双重表达**——多出来的正文条目按 C2 删除测试判定为可删除内容。
- **适用范围**:不止 `gallery`——`image-side`/`image-hero`/`two-column` 只要正文与相邻图片描述同一对象,同样适用本条。
- **与其他条目的关系**:本条是 C2(是否可以删除内容)在"图文组合场景"下的具体化判定信号,`final-quality-check.md` C2 判定标准引用本节。

<!-- TASK-025: 新增——实测发现 kpi/gallery 两个角色把"正文列表"与"该列表解析
     出的视觉组件"同时全量渲染（列表原文一字不差 + kpi-grid/gallery 图片阵列
     各自再讲一遍），是 §17.6 图文复述的姊妹问题：§17.6 管"图片说明 vs 正文"，
     本节管"正文列表 vs 从这份列表解析出的视觉卡片/图表/编号"。已在
     scripts/render_deck.py（kpi/gallery/image-hero 三处）与
     scripts/qa_render.py（新增 _list_visual_duplication 硬性门禁）修复，
     此处补规则条目使文档与代码口径一致。 -->
### 17.7 列表被消费为视觉组件时,正文不得重复渲染该列表(kpi / gallery / 任意 group-card 场景强制)

- **判定信号**:一页正文里的某个 `content-list` 列表,其条目被渲染逻辑解析、消费成了另一种视觉承载体的数据源——常见于:
  1. **kpi**:列表条目被解析成 `kpi-grid` 里的大数字卡片(数字 + 标签 + 口径);
  2. **gallery**:列表条目(每条描述一张图的风格/工艺)与 `gallery-grid` 图片阵列一一对应;
  3. **任意角色的语义分组**:列表条目被合成/登记为 `groups`,渲染成独立的 `group-card` 卡片(见 `non_list_blocks_html`)。
  一旦列表被上述某种视觉组件"消费"(即视觉组件的内容就是从这份列表条目解析出来的),正文就不能再以完整 `<ul class="content-list">` 原样打印这份列表——这会让同一批信息在页面上出现两次:先读一遍文字列表,再读一遍数字卡片/图片说明/分组卡片,对读者没有任何增量。
- **处置原则**:二者只能有一个承担"呈现这批信息"的职责:
  1. **优先让视觉组件独立承载全部信息**:数字卡片自带数字+标签+口径、图片自带 caption-bar 风格标签、分组卡片自带标题+条目,足以让读者理解,正文不再重复。
  2. **正文只保留非列表内容作为导语/总起句**(渲染层用 `non_list_blocks_html()` 取非列表块,而非 `blocks_html()` 全量输出);kpi 页导语进一步裁到一句话(`kpi_lead_html()`),因为该页的视觉焦点就是数字卡片本身,导语只是引导,不需要展开说明。
  3. gallery 页如需保留列表里的"风格判断"信息,应把核心短语(4–8 字,如"奢华航天美学")追加进对应图片自己的 caption-bar(`img_html()` 的 `caption_override` 参数),让说明文字长在图片身上,而不是单独起一段文字块逐条复述。
- **QA 门禁**:`scripts/qa_render.py` 的 `structural()` 对此有硬性检查(非提示、直接扣 50 分)——同一页里"正文列表条目数"与"该页某类视觉单元渲染实例数"完全相等(且 ≥3,避免偶然吻合)时,若渲染 HTML 中仍能找到完整的 `<ul class="content-list">` 原文,判定为重复渲染。
- **与其他条目的关系**:与 §17.6(图文复述)是同一大类问题(C2 可删除内容)在不同载体上的具体化——§17.6 针对"图片说明 vs 正文列表",本节针对"数字卡片/分组卡片/图片阵列 vs 正文列表"。
